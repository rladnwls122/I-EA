"use client";
import Link from "next/link";
import { ArrowLeft, Bookmark, Check, Clock3, MessageCircle, Pencil, X } from "lucide-react";
import { AppFrame } from "@/components/layout/AppSidebar";
import { choices } from "@/lib/mock-data";
export default function NoteDetailPage() { return <AppFrame title="오답 복습"><main className="page-content review-page"><Link href="/notes" className="back"><ArrowLeft size={16}/> 오답노트로 돌아가기</Link><div className="review-layout"><section><div className="review-head"><span className="eyebrow">문학 · 객관식 · 난이도 중</span><h1>현대시의 화자와 태도</h1><p>다음 시에서 화자의 정서와 태도를 가장 적절하게 설명한 것은?</p></div><div className="review-choice-list">{choices.map((choice,i) => <div className={`review-choice ${i === 1 ? "wrong-choice" : ""} ${i === 0 ? "right-choice" : ""}`} key={choice}><span>{i === 1 ? <X size={16}/> : i === 0 ? <Check size={16}/> : i+1}</span>{choice}{i===1 && <small>내가 고른 답</small>}{i===0 && <small>정답</small>}</div>)}</div><section className="explain panel"><span className="eyebrow">해설</span><h2>화자의 정서를 따라 읽기</h2><p>반복되는 시어와 대상에 대한 거리감을 살피면, 화자는 떠나간 대상을 기다리며 그리움의 정서를 드러내고 있음을 알 수 있습니다.</p><ol><li>대상과의 관계를 보여주는 시어를 찾는다.</li><li>반복되는 표현이 만드는 정서를 파악한다.</li><li>선지의 ‘비판’과 ‘체념’은 화자의 태도와 거리가 있음을 확인한다.</li></ol></section></section><aside className="review-side"><section className="panel review-result"><span>이번 풀이</span><b><X size={17}/> 오답</b><p><Clock3 size={14}/> 1분 38초 · 평균보다 12초 빨라요</p></section><section className="panel annotation"><div><h3>내 메모</h3><Pencil size={15}/></div><p>“화자의 정서와 태도를 분리해서 보자. 대상이 떠난 상황에 주목!”</p><span>개념 부족</span></section><button className="button"><Bookmark size={16}/> 다시 복습 목록에 담기</button><section className="panel comments"><div><h3>질문과 답변</h3><MessageCircle size={16}/></div><p>헷갈리는 부분을 남기면 함께 확인할 수 있어요.</p><textarea className="field" placeholder="질문을 남겨보세요"/></section></aside></div></main></AppFrame>; }
