"use client";
import { useState } from "react";
import Link from "next/link";
import { ArrowRight, Eye, EyeOff, Sparkles } from "lucide-react";

export function AuthForm({ mode }: { mode: "login" | "signup" }) { const [show, setShow] = useState(false); const [done, setDone] = useState(false); const signup = mode === "signup"; return <main className="auth-page"><section className="auth-aside"><Link href="/" className="landing-logo">I<span>Δ</span>EA</Link><div><span className="eyebrow">STUDY WITH INTENTION</span><h1>더 나은 질문이<br/><em>더 나은 답을</em><br/>만듭니다.</h1><p>문제를 푸는 시간부터 복습하는 순간까지, IΔEA가 학습의 다음 흐름을 함께 설계합니다.</p></div><div className="auth-quote"><Sparkles size={17}/><span>오늘의 작은 기록이<br/>내일의 확신이 됩니다.</span></div></section><section className="auth-form-wrap"><div className="auth-form"><Link href="/" className="mobile-logo landing-logo">I<span>Δ</span>EA</Link><span className="eyebrow">{signup ? "CREATE ACCOUNT" : "WELCOME BACK"}</span><h2>{signup ? "학습을 시작해볼까요?" : "다시 만나 반가워요."}</h2><p>{signup ? "필요한 정보만 입력하면 바로 시작할 수 있어요." : "나만의 학습 흐름을 이어가세요."}</p>{signup && <label>이름<input className="field" placeholder="이름을 입력하세요"/></label>}<label>이메일<input className="field" type="email" placeholder="name@example.com"/></label><label>비밀번호<div className="password"><input className="field" type={show ? "text" : "password"} placeholder="8자 이상 입력하세요"/><button type="button" onClick={() => setShow(!show)}>{show ? <EyeOff size={18}/> : <Eye size={18}/>}</button></div></label>{!signup && <div className="auth-option"><label><input type="checkbox"/> 로그인 상태 유지</label><a href="#">비밀번호 찾기</a></div>}<button className="button primary auth-submit" onClick={() => setDone(true)}>{done ? "완료되었습니다" : signup ? "계정 만들기" : "로그인"} <ArrowRight size={17}/></button><div className="auth-divider"><span>또는</span></div><button className="button auth-social">Google로 계속하기</button><p className="auth-switch">{signup ? "이미 계정이 있나요?" : "처음이신가요?"} <Link href={signup ? "/login" : "/signup"}>{signup ? "로그인" : "회원가입"}</Link></p></div></section></main>; }
